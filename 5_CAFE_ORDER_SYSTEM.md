# Java 카페 주문 시스템

TDD와 단위 테스트 학습을 위한 간단한 콘솔 기반의 카페 주문 시스템입니다.

## 📚 학습 목표
- 여러 객체 간의 협력 관계 설계
- 도메인 모델링 및 책임 분리
- 할인 정책 적용을 통한 전략 패턴 이해
- 복잡한 비즈니스 로직 테스트

---

## 🎯 기본 기능 요구사항

### 1. 메뉴판
- 시스템은 시작 시 메뉴 목록을 가지고 있어야 한다. (예: 아메리카노-4000원, 라떼-4500원, 카푸치노-5000원)
- 메뉴는 이름과 가격 정보를 가진다.

### 2. 주문하기
- 사용자는 메뉴판에 있는 메뉴를 이름으로 주문할 수 있다.
- 주문 시 수량을 함께 입력할 수 있다. (예: "아메리카노 2개")
- 주문은 여러 개의 메뉴 항목(메뉴와 수량)으로 구성될 수 있다.
- **존재하지 않는 메뉴를 주문할 경우 예외를 발생시킨다.**

### 3. 주문 내역 및 총액 계산
- 현재까지 주문한 모든 메뉴 항목과 각 항목의 가격, 총 주문 금액을 계산하여 보여줄 수 있어야 한다.

### 4. 결제
- 사용자가 특정 금액을 지불하여 결제를 시도할 수 있다.
- 지불한 금액이 총 주문 금액보다 적을 경우, "금액이 부족합니다." 라는 예외를 발생시킨다.
- 결제가 성공하면, 지불한 금액에서 총 주문 금액을 뺀 거스름돈을 계산하여 반환한다.

---

## 🚀 심화 기능 요구사항 (선택)

### 5. 재고 관리
- 각 메뉴별로 재고 수량을 관리한다.
- 주문이 들어올 때마다 해당 메뉴의 재고가 차감된다.
- 주문하려는 수량보다 재고가 부족할 경우, "재고가 부족합니다." 라는 예외를 발생시킨다.

### 6. 할인 정책 추가
- 다양한 할인 정책을 적용할 수 있도록 시스템을 확장한다.
- 예시:
    - **금액 기반 할인:** 총 주문 금액이 20,000원 이상이면 10% 할인
    - **특정 메뉴 할인:** 아메리카노 메뉴에 대해 500원 할인
- 여러 할인 정책이 중복으로 적용될 수 있도록 설계한다.
- (힌트: 전략 패턴(Strategy Pattern) 또는 데코레이터 패턴(Decorator Pattern)을 고려해볼 수 있습니다.)

---

## 🧪 테스트 케이스 예시

```java
@Test
void 존재하지_않는_메뉴를_주문하면_예외가_발생한다() {
    Cafe cafe = new Cafe();
    cafe.addMenu(new MenuItem("아메리카노", 4000));

    assertThatThrownBy(() -> cafe.order("카페모카", 1))
        .isInstanceOf(IllegalArgumentException.class)
        .hasMessageContaining("존재하지 않는 메뉴");
}

@Test
void 주문한_메뉴의_총액을_계산한다() {
    Cafe cafe = new Cafe();
    cafe.addMenu(new MenuItem("아메리카노", 4000));
    cafe.addMenu(new MenuItem("라떼", 4500));

    Order order = new Order();
    order.addItem("아메리카노", 2);  // 8000원
    order.addItem("라떼", 1);        // 4500원

    assertThat(order.getTotalPrice()).isEqualTo(12500);
}

@Test
void 결제_금액이_부족하면_예외가_발생한다() {
    Order order = new Order();
    order.addItem("아메리카노", 2);  // 8000원

    assertThatThrownBy(() -> order.pay(5000))
        .isInstanceOf(IllegalArgumentException.class)
        .hasMessageContaining("금액이 부족");
}

@Test
void 결제_성공_시_거스름돈을_반환한다() {
    Order order = new Order();
    order.addItem("아메리카노", 2);  // 8000원

    int change = order.pay(10000);

    assertThat(change).isEqualTo(2000);
}
```

---

## 💡 할인 정책 설계 TIP

할인 정책을 유연하게 적용하려면 인터페이스를 활용합니다.

```java
@FunctionalInterface
public interface DiscountPolicy {
    int discount(Order order);
}

// 금액 기반 할인
DiscountPolicy amountDiscount = order -> {
    if (order.getTotalPrice() >= 20000) {
        return (int) (order.getTotalPrice() * 0.1);
    }
    return 0;
};

// 여러 할인 정책 적용
List<DiscountPolicy> policies = List.of(amountDiscount, menuDiscount);
int totalDiscount = policies.stream()
    .mapToInt(policy -> policy.discount(order))
    .sum();
```

---

## ✅ 프로그래밍 요구사항
- 모든 기능은 TDD(Test-Driven Development) 방식으로 개발한다.
- 모든 기능 단위에 대한 단위 테스트를 작성한다.
- Java 코드 컨벤션을 지키면서 프로그래밍한다.
- 객체의 역할과 책임을 명확하게 분리한다. (예: `MenuItem`, `Order`, `Cashier`, `Inventory`, `DiscountPolicy`)
- 테스트 라이브러리는 **JUnit 5**와 **AssertJ**를 사용한다.
